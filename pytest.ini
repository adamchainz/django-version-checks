[pytest]
addopts = --cov django_version_checks
          --cov-report term-missing:skip-covered
          --cov-fail-under 100
          --cov-config setup.cfg
          --ds=tests.settings
django_find_project = false
